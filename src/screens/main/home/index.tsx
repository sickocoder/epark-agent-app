import { FC } from 'react';
import { Box, Text } from '../../../components';

const Home: FC = () => (
  <Box flex={1} center>
    <Text variant="heading">home</Text>
  </Box>
);

export default Home;
